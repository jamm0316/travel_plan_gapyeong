# 🎂 춘천 환갑여행 — 2026. 8. 22 (토)

노션 여행 계획을 기반으로 만든 정적 웹사이트. 프레임워크/빌드 없이 `index.html`, `styles.css`, `app.js`, `images/` 만으로 동작합니다.

## 실행
```bash
python3 -m http.server 8080   # 이후 http://localhost:8080
```
Vercel / GitHub Pages / Netlify 등 아무 정적 호스팅에 폴더째 올리면 됩니다.

## 기능
- 하단 글래스 네비게이션 — 8개 섹션 바로 이동, 스크롤 위치에 따라 활성 표시
- 주소록 — `복사` 버튼(클립보드), `지도` 버튼(카카오맵)
- 타임라인 — KST 기준 지난 일정 흐리게 / 현재 일정 bold + 테두리 + NOW 배지 / 남은 일정 그대로. 30초마다 갱신
- 날씨 — Open‑Meteo(무료, 키 불필요)로 춘천 실시간 날씨 + 8/22 시간별 예보. 10분마다 갱신
- 장보기 — Google Sheets 링크 카드, 케이크 픽업 주소 복사
- 준비물 / 게임 / 챙길 것 — 체크 상태가 기기(localStorage)에 저장

## 당일 화면 미리보기
`index.html?t=HH:MM` 으로 여행 당일 특정 시각을 시뮬레이션할 수 있습니다. 예: `?t=13:40`

## 수정하기
- 일정·주소: `app.js` 상단 `TIMELINE`, `ADDRESSES`
- 본문 텍스트: `index.html`
- 장소 사진: `images/` (카카오맵 장소 페이지의 대표 사진)

## PWA

- `manifest.json` + `sw.js`(오프라인 셸 캐시) 포함. HTTPS(GitHub Pages)에서 홈 화면에 추가하면 앱처럼 열립니다.
- 첫 방문 시 설치 안내 모달이 뜹니다(한 번만). 다시 보려면 `?a2hs=1`. 카카오톡 인앱에서는 외부 브라우저 열기 버튼을 안내합니다.
- 정적 파일을 바꾸면 `sw.js`의 `CACHE_VERSION`과 `index.html`의 `?v=` 값을 올려 주세요.

## 시크릿 탭

- 하단 네비 🔒 **시크릿** → 4자리 암호 `0822` 입력.
- 케이크 픽업 주소·초/라이터, 저녁 게임, 짐 목록이 여기에만 있습니다(주인공에게 보이지 않게). 잠금 해제는 탭을 닫을 때까지 유지됩니다.

## 추억 모드

- 상단 [여행 | 추억] 토글. 여행일(8/22) 이후 첫 진입은 추억 모드가 기본, 선택은 기기에 저장됩니다.
- 챕터 데이터는 `app.js`의 `MEMORY` 배열. 사진을 추가하려면 `images/memory/{dak|cafe|emart|dinner}/`에 1600px JPG를 넣고 `photos` 배열에 경로를 추가하세요.
- 스트립은 손대지 않으면 천천히 오른쪽으로 흐르고, 만지면 멈추고 직접 넘길 수 있습니다. 탭하면 크게 보기.
